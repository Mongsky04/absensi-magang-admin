# Local development:
# VITE_API_URL=http://localhost:4000

# Production (Vercel): gunakan rewrites ke /api agar tanpa CORS
# Biarkan VITE_API_URL kosong agar memakai path relatif "/api" sesuai vercel.json rewrites
# VITE_API_URL=
